let peopleAge = [20, 30, 40, 50, 60, 70, 80];

const result = peopleAge.some((x) => {
  return x >= 90;
});

console.log("result", result);

// reverse method

console.log("reverse array", peopleAge.reverse());

console.log("peoples age",peopleAge)