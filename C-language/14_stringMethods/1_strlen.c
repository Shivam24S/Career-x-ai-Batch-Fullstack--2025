

#include <stdio.h>

#include <string.h>

int main()
{

    char str[] = "hello goodMorning";

    int length;

    length = strlen(str);

    printf("the string length is %d ", length);

    return 0;
}